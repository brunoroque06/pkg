on: [push]

jobs:
  ci:
    runs-on: ubuntu-26.04-arm
    steps:
      - uses: actions/checkout@v7
      - uses: actions-rust-lang/setup-rust-toolchain@v1
      - run: make fmt-check
      - run: make build
      - run: make test
